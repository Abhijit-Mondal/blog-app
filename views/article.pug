extends layout.pug

block content 
    h2= title
    br
    p= article.body 
    h5 
        em Published by - #{author}

    hr

    if user 
        if user.id == article.author
            a.btn.btn-primary(href=`/articles/edit/${article._id}`) Edit
            a.btn.btn-danger.delete-article(href="#", data-id=article._id) Delete
